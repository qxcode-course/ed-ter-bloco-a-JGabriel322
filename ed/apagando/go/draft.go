package main
import "fmt"
func main() {
	var n int
	fmt.Scan(&n)
	identN := make([]int, n)
	for i = 0; i < n; i++ {
		fmt.Scan(&identN[i])
	}

	var m int
	fmt.Scan(&m)
	identM := make([]int, n)
	fot i = 0; i < n; i++ {
		fmt.Scan(&identM[i])
	}

	
}
